make sure already install python

1. install requirements.txt = pip install -r requirements.txt
2. create virtual environment(opsional) = - python -m venv myenv
					  - source myenv/bin/activate
4.run the migration = python manage.py migrate
3.run the app  = python manage.py runserver

thanks!